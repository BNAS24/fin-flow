const primaryFontName = "space-grotesk";
const secondaryFontName = "font-inter";

// Used for MUI theme config
const primaryFont = `var(--${primaryFontName})`;
const secondaryFont = `var(--${secondaryFontName})`;

export const themeVars = {
    fonts: {
        primary: primaryFont,
        secondary: secondaryFont,
    },
    fontSizes: {

    } ,
    colors: {

    }
}